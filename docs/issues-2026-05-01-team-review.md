# Familink 課題一覧（5 役割チームレビュー / 2026-05-01）

対象：`app-source/familink.html`（9188 行 / 単一 HTML 運用）
レビュー視点：Product Owner / UX-UI Lead / Frontend Engineer / QA Lead / Release Manager
方針：**修正は未実施**。本ドキュメントは課題の一覧化と優先度付き修正方針の提示まで。実装はユーザー確認後に着手。

---

## 総評

実装は MVP（`docs/mvp-requirements.md`）＋ Phase 2（家計 / 体調 / 準備 / カスタムボード / OCR スキャン）相当まで揃っており、**プロダクトとしての骨格は完成済み**。一方で、**App Store 公開ブロッカー級の認証・登録周りの欠落と、UI/UX ガイドライン違反の細部**が存在する。**実装の追加よりも「公開前の整理」が最優先**。

- 致命バグ（実機確認なしで断定できる）：認証ロジック / ハードコード家族 / プレミアムゲートのコピー
- ガイドライン違反：絵文字過多、用語ゆれ、Hoku 口調の微ズレ
- コード健全性：高い（`eval`/`console.log`/`TODO` ゼロ、try/catch 網羅、saveS 66 箇所）

---

## 役割別サマリ

### 1. Product Owner / PM
- **MVP 受け入れ条件（`docs/mvp-requirements.md` §2.1）「家族登録」が未実装** → 実質「単一家族デモ」状態。ストア公開できない
- 一方で Phase 2 機能（家計・体調・準備・カスタムボード）は完成済み → スコープ過剰だが既に動いているため削除はしない（UI 隠蔽の選択肢あり）
- 「無料でできること」と「プレミアムで増えるもの」の境界が UI で表現されていない

### 2. UX/UI Lead
- ホーム / ナビは家族向けにシンプルで温かい — 設計は良好
- **絵文字の過剰使用**（ガイドライン違反）：プレミアムゲートに 3 個（⭐🚫👨‍👩‍👧）、Hoku 応答に ⚠️👍、通知に ⭐🌟
- **プレミアムゲートのコピーが「アバター」に矮小化**されており 480 円の価値が伝わらない
- **用語ゆれ**：「掲示板」（実装）vs「家族ボード」「家族共有ボード」（ドキュメント / `mvp-requirements.md`）

### 3. Frontend / HTML Engineer
- 単一 HTML として綺麗にまとまっている（`H()` エスケープ完備、saveS / loadS / migrateData の 3 段階で永続化）
- **`switchTab(refresh)` で `s-cdetail` のレンダ分岐が抜け**ている（line 3320–3349）
- ログインフォームに **dev 用プレフィル**（`kenya@familink.app` / `password`）が残存（line 2030, 2034）
- 修正は最小差分で対応可能（フレームワーク変更不要）

### 4. QA Lead
- 静的解析で致命バグ S 級は検出されず（前回レポート `docs/qa-findings-2026-05-01.md` 参照）
- ただし **動的挙動として明確な High リスク** が以下に存在：
  - `doLogin()` が **パスワードを検証しない**（line 3355–3365）
  - `doQuickDemo()` が **確認なしで全データ消去**（line 3367–3377）
  - `MEMBERS` 配列が **賢弥家族の固定値**（line 2995–3001）
- 実機（iPhone Safari）での 21 画面動作確認はまだ未実施

### 5. Release Manager
- 起動環境 OK（`README.md` / `python3 -m http.server`）
- worklog / qa-findings / 役割別レビューが docs に揃いつつある
- **App Store 提出には High 級の修正が前提**（特に認証 / 家族登録 / プレミアムコピー / 絵文字）

---

## 課題一覧（優先度付き）

優先度：**High**（公開ブロッカー / 致命挙動）/ **Medium**（公開前に直したい）/ **Low**（公開後でよい）
差分規模：**XS**（1〜10 行）/ **S**（10〜50 行）/ **M**（50〜200 行）/ **L**（200 行超）

### High（公開ブロッカー）

| ID | 役割 | タイトル | 該当箇所 | 影響範囲 | 修正方針 | 差分規模 |
|---|---|---|---|---|---|---|
| **H-01** | QA / FE | `doLogin()` がパスワードを検証しない（メールが空でなければ常に `MEMBERS[0]` でログイン） | `familink.html:3355–3365` | 全ユーザーが必ず賢弥としてログインする。App Store 認証要件を満たさない | MVP は **ログイン画面を「家族プロフィール選択 + ローカル PIN」** に置換 or **「プロフィール作成」フローに置換**。クラウド認証は §10.2 で別途確認 | M |
| **H-02** | PO / FE | `MEMBERS` が賢弥家族の固定値（5 名固定） | `familink.html:2995–3001` | 公開時、ユーザーが「賢弥家」を共有することになる。プライバシー上も不可 | **初回起動時の家族プロフィール作成画面**を新設し `MEMBERS` を `S.members` に格上げ。LocalStorage に保存 | M |
| **H-03** | UX / FE | ログインフォームに dev プレフィル `kenya@familink.app` / `password` が残存 | `familink.html:2030, 2034` | スクリーンショット / ストア審査での減点。プライバシー懸念 | `value=""` に変更 + `placeholder` のみ残す | XS |
| **H-04** | QA / UX | `doQuickDemo()` が確認なしで `events / tasks / txs / posts ...` を全消去 | `familink.html:3367–3377` | 既存ユーザー誤タップで家族の記録が即消失 | `showConfirm()` でユーザー確認を必須化（既に `showConfirm` 実装あり）。ボタンラベルも「デモデータで上書き」に変更 | XS |
| **H-05** | UX | プレミアムゲートのコピーが「アバター限定」と読め、480 円の価値が伝わらない | `familink.html:8967–9000` 周辺 | 課金転換率が落ちる。`docs/premium-strategy.md` §5 と乖離 | コピーを「家族の記録をもっと便利に」を主軸に書き直し、アバターはサブ訴求に。特典 3 行のうち 1 行を「現状未実装の機能」（広告非表示）から **実装済みの差分機能** へ差し替え | S |
| **H-06** | UX | プレミアムゲートに絵文字 3 個（⭐🚫👨‍👩‍👧）— UI ガイドライン「1 画面 2 個以下」違反 | `familink.html:8985–8995` | 「安っぽい」印象。ストア審査と家族向け信頼感を損なう | 絵文字を SVG アイコンに置換（既に多数の SVG が同ファイル内にあり再利用可） | S |

### Medium（公開前に直したい）

| ID | 役割 | タイトル | 該当箇所 | 影響範囲 | 修正方針 | 差分規模 |
|---|---|---|---|---|---|---|
| **M-01** | UX / Hoku | Hoku の口調が `docs/hoku-guideline.md` から微ズレ（「だよ / のんびりできるね / 言ってね」）。ガイドラインは「しますね」系で穏やか敬語混在 | `familink.html:7195–7290` の `hokuLocalAnswer` | ブランドトーンの一貫性。ガイドラインで定めた「優しい・温かい・控えめ」と微妙に違う | 短文応答群を一括見直し（5〜10 文単位）。Hoku 一人称・語尾は `familink-hoku-ai-designer` で一括ルール化 | S |
| **M-02** | UX / Hoku | Hoku 応答に絵文字 `⚠️ / 👍` が混入 | `familink.html:7208, 7271` | UI ガイドライン違反 | 絵文字を文字（「期限切れ」「順調です」）または SVG に置換 | XS |
| **M-03** | FE | `switchTab(refresh)` で `s-cdetail` のレンダ分岐が抜けている | `familink.html:3320–3349` | 子ども詳細画面に切り替えても再レンダされない可能性。実機要確認 | `case 's-cdetail': renderChildDetail(); break;` を追加（関数名は実体に合わせる） | XS |
| **M-04** | PO / UX | 用語ゆれ：「掲示板」(UI 9 箇所) vs「家族ボード / 家族共有ボード」(docs) | `familink.html:2394, 2978, 3645, 5874` ほか | ブランド統一感。`docs/mvp-requirements.md §2.5` と乖離 | UI ラベルとドキュメントを「家族ボード」に統一する案が有力（`familink-core` で裁定） | S |
| **M-05** | PO / UX | ブランド表記ゆれ：「ファミリンク」(3 箇所) と「Familink」(12 箇所)。`<title>` はカナ | `familink.html:9` ほか | ストア・SNS 拡散時のブランド一貫性 | 「Familink」に統一 + 必要なら `<title>Familink ファミリンク</title>` のように併記。`familink-core` で確定 | XS |
| **M-06** | UX | オンボード（s-ob）の CTA 2 つ（「はじめる」「すでにアカウントをお持ちの方」）が両方とも `go('s-login')` で同一遷移 | `familink.html:2009, 2013` | UX 上の選択肢が機能していない。新規 vs 既存で導線分岐が望ましい | H-02 の家族プロフィール作成と連動。新規 → 作成画面、既存 → ログイン | S |
| **M-07** | UX | 通知に絵文字 ⭐ + 🌟 が同行に（`'⭐ ファミリンクへようこそ！'` + `icon:'🌟'`） | `familink.html:3476` | 装飾過多 | アイコンを SVG 化 or どちらか 1 つに | XS |
| **M-08** | QA | `m-confirm` のデフォルトアイコンが ⚠️ | `familink.html:2912, 3225` | 削除以外の確認でも警告色 | `showConfirm()` 呼び出し側で意味に応じてアイコン分岐（情報 / 警告 / 削除） | XS |

### Low（公開後でよい）

| ID | 役割 | タイトル | 該当箇所 | 影響範囲 | 修正方針 | 差分規模 |
|---|---|---|---|---|---|---|
| **L-01** | FE | 画像 base64 1.3MB の単一 HTML — 4G 初回ロードが重い | 全体 | 初回起動時間 | Phase 2 候補：`public/images/` への外出し + キャッシュ戦略 | M |
| **L-02** | FE | XSS サーフェス点検：`${...}` 補間 741 件中 `H()` エスケープ 187 件。ユーザー入力（name/title/merchant/memo/note）の経路だけ抜き打ち確認 | 全体 | 単一家族 LocalStorage 運用なので XSS 影響は限定的だが、念のため | grep で「ユーザー入力名」を含む `${...}` を抽出し、`H()` 漏れを潰す | S |
| **L-03** | QA | `addEventListener` 47 vs `removeEventListener` 12。画面切替で再バインドされる listener にメモリリーク懸念 | 全体 | 長時間運用時のパフォーマンス | 画面切替でアタッチされる listener を抽出し、必要なら removeEventListener を追加 | S |
| **L-04** | PO | `seedDemo()` が空時のみ走るが、ユーザーが手動で全データ削除した直後にアプリ再起動するとデモが復活する可能性 | `familink.html:3391` ほか | 「全消去したのにデータが戻る」というユーザー驚き | `S._demoSeeded:true` フラグを LocalStorage に保存し、二度目の seed をスキップ | XS |

---

## 推奨着手順（実装は未実施）

ユーザーの GO 後に、以下の順で最小差分で進める案：

### 第 1 弾（公開ブロッカー解消 / 推定半日〜1 日）
1. **H-03** ログインプレフィル削除（XS / 即効）
2. **H-04** デモデータ確認モーダル追加（XS / 即効）
3. **H-06** プレミアムゲート絵文字 → SVG 置換（S）
4. **H-05** プレミアムゲートコピー書き直し（S / `familink-monetization-lead` レビュー要）

### 第 2 弾（認証 / 家族登録 / 推定 1〜2 日）
5. **H-01 + H-02 + M-06** を一括で対応：「家族プロフィール作成 + プロフィール選択 + ローカル PIN」フロー新設。`MEMBERS` を `S.members` に格上げ。
   - 実装前に **`familink-cto-architect` で LocalStorage 構造変更の影響範囲確認**（PERSIST に `members` 追加、デモ家族との互換維持）
   - `familink-requirements-architect` で受け入れ条件再定義（`docs/mvp-requirements.md` §2.1 を更新）

### 第 3 弾（用語 / 文言整理 / 推定半日）
6. **M-01 + M-02** Hoku 文言一括見直し（`familink-hoku-ai-designer`）
7. **M-04 + M-05** 用語統一（`familink-core` で裁定 → 一括置換）
8. **M-07 + M-08** 細かな絵文字 / アイコン整理

### 第 4 弾（軽微修正 / 推定 0.5 日）
9. **M-03** `s-cdetail` のレンダ分岐追加

### 実機 QA（並走）
10. iPhone Safari で `docs/qa-findings-2026-05-01.md §3` の 12 項目を実走 → S 級発生時は最優先で着手

### Phase 2 以降（公開後）
- L-01〜L-04 を順次対応

---

## 着手前に必ずユーザー確認したい項目

1. **H-01 / H-02 の実装方針**：以下のどれにするか
   - (a) ローカル PIN + プロフィール作成のみ（クラウド認証なし、最短）
   - (b) Supabase 等のクラウド認証を入れる（CLAUDE.md §10.2 該当 / 工数大）
   - (c) MVP では「プロフィール選択のみ、PIN なし」で割り切る
2. **H-05 のコピー方針**：プレミアム特典として **実装済み機能のうち何を有料線引き** にするか（`docs/premium-strategy.md` §4 を参照しつつ、現実装で動く差分機能の選定）
3. **M-04 の用語**：「掲示板」→「家族ボード」への統一で OK か（既存ユーザーの LocalStorage には影響なし、UI ラベルのみ）
4. **iPhone 実機 QA の実施タイミング**：第 1 弾の前 or 後

---

## 参考

- `CLAUDE.md` §10（完全自動化・ユニコーン方針）
- `docs/mvp-requirements.md`（MVP 受け入れ条件）
- `docs/ui-ux-guideline.md`（絵文字・用語ルール）
- `docs/hoku-guideline.md`（Hoku 口調 / UI）
- `docs/premium-strategy.md`（プレミアム戦略）
- `docs/qa-findings-2026-05-01.md`（前回静的 QA レポート）
