# Familink 携帯運用ガイド

PC を開けない時間帯、iPhone から claude.ai/code 等で短文指示だけで Familink を運用するためのガイドです。
ルールの根拠は `CLAUDE.md` §8「携帯短文指示への対応」を参照。

---

## 1. 携帯運用の前提

- iPhone から長文は打てない / レビューも短時間
- Claude Code 側が Skill を自動選択して動く
- 開始 / 終了プロトコルだけは絶対に守る（短文でも）
- 大規模変更は独断で進めない（必ず `要事前確認` に倒す）

---

## 2. 携帯から使える短文コマンド集

| 携帯から送る一言 | Claude Code の動き |
|---|---|
| 作業開始確認して | `git status` / 最新コミット / 前回 worklog を確認 → 【作業開始確認】を返す |
| 続き / 次 / 進めて | 前回 worklog の「次にやること」へ着手 |
| 優先度 S だけ総点検して修正して | qa-lead → debug-engineer → html-engineer の順で S だけ |
| 総点検して | qa-lead で全体の手動テスト計画 |
| iPhone 確認ポイント出して | qa-lead で iPhone 観点のみ列挙 |
| この画面プロっぽくして | uiux-designer で改善案 |
| Hoku の文言直して | hoku-ai-designer で 2〜3 案 |
| バグ原因見て | debug-engineer で再現 → 仮説 → 修正案 |
| 最小変更で実装して | html-engineer で最小差分 |
| まだコード変更しないで | 設計 / 提案のみ。Write/Edit を保留 |
| まとめて / 短く報告して | chief-review-officer で 3 件以内に圧縮 |
| 作業終了して。worklog 更新してコミットして | master-controller で終了プロトコル |
| 閉じて / 一区切り / コミットして | 同上（終了プロトコル） |

---

## 3. 推奨運用パターン

### パターン A: 様子見だけ
```
作業開始確認して
↓
（Claude Code が現状報告）
↓
何もせず閉じて
```

### パターン B: S バグだけ潰す
```
作業開始確認して
↓
優先度 S だけ総点検して修正して
↓
iPhone 確認ポイント出して
↓
作業終了して。worklog 更新してコミットして
```

### パターン C: 改善 1 件だけ進める
```
作業開始確認して
↓
次の A 改善を 1 件だけ進めて
↓
まとめて
↓
作業終了して。worklog 更新してコミットして
```

### パターン D: 設計だけ進める（コードは触らない）
```
作業開始確認して
↓
〇〇機能の要件定義を出して。まだコード変更しないで
↓
chief-review-officer で 3 件以内にまとめて
↓
作業終了して。worklog 更新してコミットして
```

---

## 4. 携帯から「やってほしくないこと」を伝える短文

| 短文 | 意味 |
|---|---|
| まだコード変更しないで | 提案のみ、Write/Edit 保留 |
| 大規模変更しないで | 要事前確認カテゴリは禁止 |
| LocalStorage 触らないで | 構造変更禁止 |
| Hoku のデザイン変えないで | 既存 Hoku デザイン保持 |
| 最小変更で | 関連箇所の同時リファクタリング禁止 |

---

## 5. 携帯レビュー時に Claude Code に求めるべき出力

Claude Code 側は携帯レビュー前提で **短く** 出力すること。基準：

- 1 画面で読める長さ（だいたい 20 行以内）
- 判断が必要な点は **最大 3 件まで**
- 各件は 1 行で書く（YES / NO / 保留 で答えられる粒度）
- 詳細を求められたら別途展開

迷ったら `familink-chief-review-officer` を最後に通す。

---

## 6. 端末またぎ時の注意

- iPhone で作業 → 終わる前に必ず push
- PC に戻ったら `git fetch origin` → `git status -sb` → 末尾 worklog 確認
- worklog の `env:` フィールドで端末を必ず明示
- 同時並行で PC と iPhone から触らない（ブランチ衝突回避）

---

## 7. 困ったときの 1 行

困ったら以下のどれかを送る：

- 「作業開始確認して」← 何もせず現状だけ知りたい
- 「まとめて」← 出力が長くて読めない
- 「まだコード変更しないで」← 危険そうな動きを止める
- 「作業終了して。worklog 更新してコミットして」← とにかく安全に閉じたい
