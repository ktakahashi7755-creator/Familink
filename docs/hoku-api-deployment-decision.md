# Hoku API デプロイ — 決定ドキュメント（A7）

最終更新：2026-05-16 / 状態：**推奨案提示（オーナー最終判断待ち）**

`hoku-api/`（FastAPI + Pydantic）は Phase 2-3 まで実装済み。フロント側の
`callHokuApi()` も配線済みだが、**既定では休眠**（設定画面で URL 未入力 →
従来 100% ローカル動作）。本書はデプロイの要否と方式を整理する。

---

## 1. 結論：**MVP 公開時点ではデプロイ不要**

- 現状の Hoku は**ローカルのルールベース分類**で 7 シナリオすべて PASS。
  API がなくてもアプリは完全動作する。
- API を足す価値は「分類精度の底上げ」だが、MVP では**ローカルで十分**。
- デプロイは**サーバー費用・運用・障害対応**が発生する。MVP 公開の
  クリティカルパスに乗せる必要はない。

→ **推奨：MVP は API なしで公開。デプロイは公開後、LLM 連携と同時に検討。**

---

## 2. 将来デプロイする場合の方式比較

| 方式 | 無料枠 | 長所 | 短所 |
|---|---|---|---|
| **Render** | あり（スリープあり）| FastAPI を Git 連携で簡単デプロイ / 無料枠で検証可 | 無料枠は一定時間でスリープ → 初回応答が遅い |
| **Fly.io** | 小規模無料 | 低レイテンシ / スケール柔軟 | 設定がやや複雑 |
| **Cloudflare Workers** | 寛大な無料枠 | 高速 / スリープなし | Python FastAPI をそのままは動かない（要書き換え）|
| **Railway** | トライアルのみ | 簡単 | 継続利用は有料 |

### 将来の推奨：**Render（検証）→ 本番化時に Fly.io 検討**
- まず Render 無料枠で動作検証（`hoku-api/` をそのままデプロイ可能）。
- 本番で常時起動・低レイテンシが要るなら Fly.io へ移行。

---

## 3. デプロイ後の有効化フロー（コードは実装済み）

1. `hoku-api/` をデプロイ → 公開 URL を取得（例：`https://hoku-api.onrender.com`）
2. Familink 設定画面「Hoku 連携（実験的）」で API URL を入力・保存
3. 以降、Hoku の分類は API を経由（失敗時は自動でローカルにフォールバック）
4. 実機で 7 シナリオを検証

> フロント側 `callHokuApi()` は 3 秒タイムアウト + 失敗時フォールバック済み。
> API がダウンしてもアプリは壊れない設計。

---

## 4. セキュリティ前提（実装済み・維持すること）

- API キー（LLM / アクセス制御）は**環境変数のみ**。コード・フロントに直書き禁止。
- `/api/hoku/intent` のログは intent 種別のみ。本文（個人情報を含みうる）は残さない。
- `.env` は `.gitignore` 済み。コミットは `.env.example` のみ。

---

## 5. オーナーへの確認事項

1. MVP は **API なしで公開**でよいか（推奨）
2. 将来デプロイする場合、Render 無料枠での検証から始めてよいか
3. LLM 本連携（`hoku-api/classifier` の LLM 分岐）を行うか — API キー費用が発生

> 本書は方式整理と推奨。デプロイ・LLM 連携の着手はオーナー確認後
> （CLAUDE.md §10.2「外部サービス連携追加」に該当）。
